# 内容写作规范（CONTENT GUIDE）

本文件是 HyiBody 内容资产的写作与审核标准。目前覆盖两类内容：

- **信息卡文案**（`content/definitions/{zh,en}.json`）——点开一个结构时显示的"它是什么"
- **奥秘**（`content/wonders/*.json`）——一段可播放的三维讲解，"它正在做什么"

> **命名待定**：`wonder` / 「奥秘」这个叫法正在改。改名会同时动 i18n、文件名、URL 与
> UGC 界面，定下来之前本文件沿用旧称，代码里的 `Wonder` 类型也不动。

规范的来源：2026-08-19 由四个专家 agent 对全部 189 条信息卡文案与三条脚本做了一轮
审核（解剖事实、英文准确性与中英一致性、中文文风、脚本结构），107 条发现里
77 条已套用。下面的硬规则大多能对应到一条自动化测试。

---

## 一、信息卡文案

### 字段与长度

| 字段 | 作用 | 中文上限 | 英文上限 |
|---|---|---|---|
| `blurb` | 信息卡正文，**一句话**说清"是什么、在哪、干什么" | 90 字符 | 190 字符 |
| `fact` | "你知道吗"，要有**可复述**的信息点 | 120 字符 | 240 字符 |

单行，不许换行。中英一一对应，`tests/unit/definitions.test.ts` 双向把关。

### 硬规则

1. **事实第一**。数字、位置、归属、机制都要能对上主流教科书。拿不准就换一个能确定的说法
2. **不传播已被推翻的说法**。已经踩过的坑：舌头味觉分区图、"眼球一生尺寸不变"、
   小肠表面积"一个网球场"（实测约 30 m²，2014 年 Helander & Fändriks）、
   肺表面积"半个网球场"（70 m² 对不上，网球场约 260 m²）
3. **不做医学建议**。可以说"医生常靠它判断"，不要说"建议你如何如何"。
   涉及急救与临床操作的说法要标明争议（如环状软骨加压已不再被指南推荐常规使用）
4. **一个知识点只在一处出现**。`fact` 不许是 `blurb` 的换句话说；相邻结构之间
   （肝门 vs 肾门、升结肠 vs 降结肠）不许用同一个句子模具
5. **比喻要准确，且不用被用滥的**。已下架：人体的第二心脏、信息高速公路、
   司令部/总指挥这类官职比喻、生造绰号（"背阔肌的小帮手""屈肘的老黄牛"）
6. **中文断句要念得通**。踩过的坑："锥体的尖叫肾乳头"、"下行为小腿供血"、
   "在肘部分出"——写完自己念一遍
7. **英文是英文，不是翻译腔**。全站统一英式拼写（defence / metres / oesophagus /
   caecum / disc / coeliac）。只在英语里成立的梗（funny bone）要说明它是英语俗名，
   不能写成"everyone calls it"

### 数字体例

同类概数统一用阿拉伯数字（"100 多万根"而不是"一百多万"）；字母形状不加引号
（`S 形`、`C 形`）；数字与单位之间留一个空格。

---

## 二、引导游览脚本

### 五拍骨架

每条脚本照这个骨架写。两档节奏：**单器官/单主题 5–9 步 / 45–110 秒**，
**系统级旗舰 10–14 步 / 90–120 秒**（2026-08-19 放宽：原来单器官上限是 8 步 / 75 秒，
但补齐五拍、每一拍都点名一个真实结构之后，八九步是自然长度，十则新奥秘全部落在
89–108 秒，硬压回 75 秒只能靠砍掉"连线"那一拍）。

| 拍 | 做什么 | 典型参数 |
|---|---|---|
| **定场** | 全身宽景，抛出钩子，先不选中任何结构 | `preset: 'hero'`，`layer ≈ 0.05` |
| **认形** | 选中整个器官：它长什么样、在哪 | `selected`，`focus: true`，需要换角度时加 `from` |
| **拆内** | 打开或切开，看进去 | `expand` 或 `clip` |
| **连线** | 它的血管 / 神经 / 管道邻居，说明它怎么接进身体 | 换 `selected`，调 `layer` |
| **收尾** | 拉回宽景，一句带走，邀请自由探索 | `preset: 'hero'`，不设 `selected` |

**收尾这一拍不能省**：`WonderEngine` 结束时只重置显隐、不动相机，没有收尾步的话
最后一帧就是"所有结构在一个特写周围突然弹回来"。

### 硬规则（都有对应的自动化测试）

1. **剖切面必须切到主角**。`clip.pos ∈ [-1, 1]` 映射的是**整具人体**的包围盒，
   不是结构自己的——手写这个数几乎必错。用 `clipPosForCoordinate` 从目标 bbox
   中心反算。心跳之旅第 4 步曾把 pos 写成 0.52，切面落在 y ≈ +76 mm 而心脏在
   y ∈ [-72, 27]，整颗心被切掉，11 秒白屏
2. **主角必须看得见**，这件事有三层，缺一层就是对着空白讲解：

   a. **系统不透明度**：选中结构所属系统在该 `layer` 下要大于拾取阈值 0.15。
      神经之旅第 2 步曾在 `layer 0.9` 选中垂体（属器官层，此时已淡到 0）。
      有测试。

   b. **内部件要展开父结构**：有 `parent` 的结构不 `expand` 父结构时不透明度直接是 0
      （`HyiViewer.effectiveOpacity`）。有测试。

   c. **不能被别的实心结构挡住**。这一层最容易漏，因为前两层都过得去。
      2026-08-19 专家审核用真网格做屏幕空间遮挡光栅化，量出实际可见比例：
      肾锥体 **0.0%**（整个躲在肾皮质里）、肱二头肌 4.1%、胫骨前肌 10%、
      晶状体 18.9%、支气管树 22.8%、膀胱 34.2%、心脏 43%。
      三种解法：`clip` 剖开外壳、`systems: { organs: 0.35 }` 压暗挡路的系统、
      `from` 换一个方向看。**注意 `from` 会被后续步骤继承**——上面一半的问题
      都是某一步写了 `from: 'back'`，后面几步的正面肌肉跟着在背面拍。
      只有"被同组兄弟件的包围盒整个套住"这一种有测试兜底，部分遮挡仍需人或
      agent 复核。

   d. **展开期间兄弟件一律不透明**：`expand` 会顺带 `isolate`，非本家结构压到 0.015
      （等于幽灵），所以那一步的文案不能点名家族之外的结构
3. **相邻两步不许是同一帧**。至少在 `selected` / `expand` / `clip` / `preset` /
   `systems` 里改一项，或 `layer` 变化 ≥ 0.1
4. **文案不许和画面打架**。神经之旅第 4 步曾一边说"藏在椎骨管道里"，一边
   `systems: {skeleton: false}` 把椎骨关掉了。同类踩坑：脊柱之旅定场用
   `preset: 'back'` 却让观众"看侧面那四道弯"；胸廓第 7 步说"前面正好有胸骨挡着"
   而镜头方向是从背面继承来的
5. **展词里不许有生产备注**。"数据将在后续版本补充"这类话写进 `docs/STATUS.md`，
   不写给观众
6. **中英并行写、并行审**。英文必须承载与中文相同的具体意象，不能一边是画面
   一边只剩定义。测试按字符数比值（英文 / 中文 > 1.2）粗筛漏译
7. **数字只能有一个出处**。脚本里出现的数字，若 `definitions` 里同一 slug 也有，
   必须逐字一致

### 三个容易用错的字段

- **`preset`** = 整具人体的宽景（正面 / 背面 / 左侧…），用于定场与收尾
- **`from`** = 只定观察方向，距离仍由 `focus` 按结构包围盒算。
  "从左侧凑近看二尖瓣" = `from: 'left'` + `focus: true`，与 `preset: 'left'` 不是一回事
- **`systems`** = `false` 关掉、数字 0–1 压暗、缺省完全可见。
  "在肋骨里面看心脏"写 `systems: { skeleton: 0.2 }`，不要写 `false`——关掉就没有"在里面"了

### 节奏

`durationMs` 用公式算而不是手填：

```
clamp(7000, round((4500 + 130 × 中文字数) / 500) × 500, 14000)
```

**中文字数 = 去掉空白后的字符数，含标点。**（按纯汉字算每一步会短 500 ms，
两种算法都自洽，写明是为了复核时对得上。）

本步若改变了 `expand` / `clip` / `preset`，再 `+1500 ms` 留给相机飞行。

### 文案分工

信息卡负责"**它是什么**"，脚本负责"**它正在做什么、下一步去哪**"。
脚本不得复述 `definitions` 里的 `blurb` / `fact`——观众会先后看到两处，重复很刺眼。

**有测试**：某步展词与它 `selected` 结构的 `blurb + fact` 之间，去掉非汉字后的
最长公共子串不得超过 **7 个汉字**。留到 7 是因为数字必须与信息卡逐字一致
（"每天跳动约 10 万次"），这类重合是规范要求的。2026-08-19 专家审核在十则新奥秘里
查出 21 处复述，最长逐字重合 14 字——光靠自觉不行。

### 每一步只能点名画面上有的东西

播放脚本时 `StructureLabel` 与 `InfoCard` 都被隐藏，展词是屏幕上唯一的标签。
所以每步文案必须点名当前 `selected` 的那个结构，也不许点名画面上没有的结构。

---

## 三、规模化之前要先做的事

要从 3 条扩到每个器官都有，下面几项得先落地，否则写完 100 个文件再改要改 100 次：

- [x] **自动收录**：`src/wonders/index.ts` 已改成 `import.meta.glob`，加文件即生效，
      顺序按文件名排（稳定）。器官级奥秘的文件名用主结构 slug（`kidney.json`）
- [x] **深链**：`?wonder=<id>` 直接开播；id 不存在时安静回到普通视图
- [x] **元数据**：`system` 已加（菜单按系统分组用）；时长由 `estimatedSeconds()` 从
      `durationMs` 求和算出，不再手填。还缺 `level`（少儿 / 成人）
- [x] **菜单按系统分组**：按分层顺序（由外到内）分组，每条显示时长
- [x] **`structures` 字段与反向索引**：每条奥秘声明它讲到哪些结构，
      `wondersForStructure(slug, parent)` 建反向索引；内部件没有自己的内容时回退到
      父结构。信息卡上已经显示「关于它的 N 则奥秘」，点一下直接开播
- [x] **`systems` 支持数值**：`false` 关掉、数字 0–1 压暗、缺省完全可见。
      "在肋骨里面看心脏"写成 `systems: { skeleton: 0.2 }`；退出奥秘时会还原
- [x] **机位方向与距离解耦**：新增 `from: 'front' | 'left' | …` 只定方向，距离仍由
      `focus` 按结构包围盒算。`preset` 保留原义（整具人体宽景），两者不再互斥
- [x] **收起时相机改成飞回去**：`frameContent(immediate)` 只在首次加载时硬切，
      收起隔离/展开时改为 `flyTo`，不再弹一下
- [x] **UGC 前置**：`content/schema/wonder.schema.json`（draft 2020-12）是投稿的校验依据，
      内置内容自己先跑这条校验（`tests/unit/wonders.test.ts` 用 ajv 验）；
      每条奥秘带 `schema: 1` 版本号；`id` 进 `?wonder=` 深链，定了就不要改
- [ ] **还没做**：`level`（少儿 / 成人）、投稿与审核流程、作者署名与许可证声明

---

## 四、审核流程

新增或改动内容后，跑一轮专家审核：

1. **解剖事实**（中文）——只挑事实错误，不碰文风
2. **英文准确性与中英一致性**——含翻译腔与文化专属梗
3. **中文文风**——句式重复、比喻陈旧、`fact` 空洞、断句歧义
4. **脚本结构**——叙事弧、镜头语言、节奏、与信息卡的分工

审核产出统一格式（`slug` / `field` / `severity` / `current` / `suggested` / `reason`），
套用时**只在 `current` 与文件原文完全一致时才替换**，这样多份报告改到同一句时后者
自动跳过，由人裁决，不会互相覆盖。事实审核优先于文风审核。
