# 项目进度（STATUS）

最后更新：2026-08-20 · 十则新奥秘 + 四轮专家审核 + 移动端界面修复（v0.4）

## 已完成（本会话，issue #1–#5）

- `pipeline/config/sources.yaml`：BodyParts3D 4.0 八个文件直链（NBDC 存档站
  `/data/bodyparts3d/LATEST/`，即 4.0 终版）+ 实测 sha256 + CC BY 4.0 许可证信息
- `download.py`：下载 → sha256 校验（不符报错退出）→ 解压；重复运行跳过已有文件
- `select.py` + `config/groups.yaml`：按 KICKOFF 第 7 节规则生成
  `content/structures.candidates.yaml`（132 个候选：骨骼 35、肌肉 42、血管 29、
  器官 19、神经 6、皮肤 1），含面数/包围盒体积/父概念参考信息
- `process.py`：合并元素 OBJ → 焊接清理 → fast-simplification 减面 → isa 全集
  包围盒中心统一居中（各系统跨运行对齐）
- `export.py`：pygltflib 组 glb（节点名 = slug，extras 按 KICKOFF 第 7 节）→
  gltf-transform dedup/weld/quantize/meshopt → `manifest.json`（含 BP3D 署名）
- `validate.py` + `run_all.py`（`pnpm pipeline:all`）；pytest 19 个用例覆盖表解析、
  分组匹配、配置校验（`pnpm pipeline:test`）
- **全六系统流水线产物已提交**：135 结构 / 6 个 glb 共 **1.87 MB** / 239,044
  三角面（骨骼 0.52 + 肌肉 0.45 + 器官 0.42 + 血管 0.31 + 神经 0.07 + 皮肤 0.05 MB），
  首屏（皮肤+骨骼+manifest）0.63 MB ≤ 5 MB，校验全绿；`run_all.py` 默认跑全系统
- 查看器接入：GLTFLoader 注册 MeshoptDecoder；占位生成脚本检测到流水线 manifest
  即跳过；页脚显示 BP3D 署名；Playwright 截图冒烟通过（骨骼完整渲染）

## 结构挑选方法（供人类审阅候选清单）

规则写在 `pipeline/config/groups.yaml`（人可编辑）：每个候选结构 = 若干 BP3D 概念
（英文名精确匹配或正则），元素网格按 FJ id 去重合并。骨骼按 KICKOFF：颅骨 13 块合
为一组、椎骨按颈/胸/腰三组、肋骨整体一组、腕骨/跗骨各一组、四肢长骨左右单列；
isa 集缺的整器官（心/肺/肝/脑/主动脉/胸骨等）取 partof 集（`source: bp3d_partof`）。
审阅方式：直接编辑候选 yaml 删条目/改字段后另存为 `content/structures.yaml`，或改
`groups.yaml` 重跑 `python3 pipeline/select.py`。`meta` 块（面数/体积/父概念）仅供
参考，定稿时可删。未匹配与剔除记录在 `pipeline/work/select_report.json`。

**BP3D 4.0 已知缺口**（两集均无网格，候选清单已如实缺席，详见 DECISIONS.md 待定节）：
尾骨、甲状腺、子宫/卵巢（男性模型）、全部主要周围神经（坐骨神经/臂丛等；nerves
目前只有脑/小脑/脑干/脊髓/视神经/神经系统复合体 6 条）、腹直肌/背阔肌/咬肌等部分浅层肌。

## 已完成（M1-6，issue #6，PR #16）

- 首屏只加载皮肤+骨骼（0.63 MB）即可交互，其余四系统后台补载
- 分层滑块 0–1 连续透视（layers.ts 映射；皮肤/肌肉用菲涅尔 X-ray 材质）
- 点击识别（拖拽不误触；仅命中不透明度 > 0.15 的结构）+ 悬停高亮 + 选中青色高亮
- 信息卡：中英名 + FMA、系统/部位/来源署名、隔离/隐藏/聚焦/关闭；一句话科普
  为占位文案（等 content/definitions，人类任务）
- 系统面板：每系统显隐 + 透明度；血管按名称自动分动脉红/静脉蓝
- 单剖切面（左右/前后/上下 + 位置滑块）；6 预设视角 + 相机平滑飞行
- 搜索（中英子串，中文命中优先，点选即选中+聚焦）；署名弹层
- URL 状态：?v= 恢复与防抖回写（分层/系统开关/剖切/选中/相机）
- Zustand store 桥接 viewer（src/viewer 仍零 React 依赖，ESLint 强制）
- 测试：23 个单测（新增搜索、剖切数学）+ 2 个 e2e（默认视图；?v= 恢复分层+选中断言信息卡并截图）

## 已完成（观感升级包，PR #17）

- 网格精度 ×3：总面 23.9 万 → **59 万**（预算 150 万），总体积 4.1 MB（预算 40 MB），
  首屏 1.31 MB（预算 5 MB）；面数预算修订记入 CLAUDE.md 与 DECISIONS.md
- 软组织（皮肤/肌肉/器官）减面后 Taubin 平滑，消低模碎裂感
- 渲染：ACES 色调映射 + RoomEnvironment 环境光照；分系统 PBR 质感
  （骨骼哑光 / 器官血管清漆湿润 / 神经缎面）；渐变舞台背景；选中反壳描边

## 已完成（M1-7 移动端与手感，issue #7，PR #18）

- 小屏（≤720px）：右侧面板改抽屉（系统/视角标签唤出，选中结构自动收起）、
  信息卡改底部全宽卡片、搜索/滑块全宽；触屏点按目标 ≥ 44px
- 选中后相机平滑对准（保持距离与方向，只移目标点）
- 性能预算检查进流水线校验：draw call 静态估算（结构数 ≤ 580）加入 validate.py
  （体积/面数已有；fps 需真机人类确认）
- e2e 新增移动端视口用例（390×844：抽屉展开 + 信息卡断言 + 截图）

## 已完成（M2-1 奥秘 + 科普文案，加强路线第③步，随 PR #18）

- **135 条一句话科普 AI 初稿**（`content/definitions/zh.md`，按 slug 分节，
  **待人类校对**——改 md 提交即生效）；信息卡已接入显示
- Wonder 引擎完善（声明式步骤：分层值+选中+聚焦/预设+显隐覆盖，自动播放计时、
  播放/暂停/上一步/下一步/退出）
- 三条奥秘（AI 初稿文案，**待人类审核**）：心跳与血液的旅程（7 步）、
  食物去哪了（7 步）、一根神经的旅程（5 步，周围神经缺口在文案中注明）
- 播放器 UI（暖色奥秘按钮 + 底部文案卡）；播放中隐藏信息卡/滑块，退出恢复
- 测试：+9 单测（md 解析、文案×清单双向契约、奥秘引用结构存在性、
  引擎状态机假定时器）+1 e2e（菜单启动心跳之旅、步进、退出、截图）

## 已完成（加强路线第④步，随 PR #18）

- 心跳搏动（66 次/分，"扑通"波形）+ 双肺呼吸起伏（13 次/分）微动画，
  绕结构中心 ≤3% 缩放，选中描边跟随；`prefers-reduced-motion` 自动关闭
- 剖切开启时材质双面渲染，断面露出内壁呈"实心感"（真 stencil 封盖列入待定）
- CC0 皮肤替换未做：涉及新数据源与域名白名单，记入 DECISIONS 待定节

## 已完成（M2-2 Kiosk + M2-3 分享，PR #19 已合并）

- Kiosk 展厅模式（`?kiosk=1`，`?idle=秒` 调闲置阈值默认 60）：大点按目标 ≥56px、
  禁右键；闲置进入吸引动画（自动旋转 + 44s 分层扫描循环 + 品牌浮层），触摸即退出
- 分享弹层：当前视图状态编码进 `?v=` URL + 二维码（qrcode 依赖，MIT）+ 复制链接；
  分享链接剔除 kiosk 参数
- 测试：+3 单测（吸引扫描波形）+1 e2e（?idle=2 加速吸引动画、触摸退出、二维码渲染）

## 已完成（M2-3 PWA + M2-5 中英切换，PR #20）

- PWA 离线：vite-plugin-pwa（autoUpdate），预缓存全部资产 4.95 MB；
  安装图标为脚本生成占位（人类可换 public/icons/）
- 中英切换：顶栏 EN/中文 按钮实时切换全部界面文案与奥秘文案；
  语言随 ?v= 分享；zh/en 键集一致性单测保证
- 英文态一句话科普暂显示占位句（en 定义文件待补，见 DECISIONS）

## 已完成（渲染与网格问题修复，2026-08-18 · 人类截图反馈）

人类反馈"背景颜色以及人体细节"有问题，两张截图定位到三个真 bug + 三处观感欠账：

- **长刺（肩膀/手脚放射状尖刺）**：`trimesh.smoothing.filter_taubin` 在本数据上发散——
  三角肌包围盒 499 → 651 mm、最长边 65 → 409 mm，肝脏 305 → 571 mm。改成自实现的
  伞算子 Taubin（固定边界点 + 位移硬上限 1% 对角线），最长边/对角线最大比例
  0.89 → 0.60（剩下的是 BP3D 源网格本就粗的血管短桩）。
  加了三道防线：process.py 包围盒涨幅 > 3% 直接报错、validate.py 长边比例
  > 65% 报错 / > 40% 警告、pipeline 合成球单测（5 个）。
- **选中皮肤后整个视口变青色**：描边反壳的外扩量写在物体空间，而流水线用
  KHR_mesh_quantization、节点自带缩放（皮肤那条 859.88），2.2 被放大成 1891 mm 巨壳。
  改为视图空间按深度换算的固定像素宽（2.5 px）。
- **?v= 分享链接选中器官/血管/神经会丢**：这些系统首屏之后才加载，选中被静默丢弃。
  viewer 现在先挂起选中、等对应系统加载完补上。
- **画面糊成一团白**：分层滑块此前让六个系统在 layer=0 时全是 1，六层实体叠加被
  ACES 压爆。改成 KICKOFF 说的"淡入淡出"：轮到自己之前只有 0.12–0.3 底噪。
- **肌肉层看不出肌肉**：肌肉从 X-ray 菲涅尔壳改为受光材质（皮肤仍是 X-ray）。
- **相邻结构分不出边界**：同系统内按 slug 做确定性微小色相/明度抖动。
- **精度**：135 个结构里 60 个已是 BP3D 源网格原分辨率；把压缩比 ≥ 6× 的 20 个
  （肋间肌 42×、腹外斜肌 37×、脑 16×、肝 13× 等）target_faces 翻倍，单件封顶 6,000、
  合并组/整器官封顶 24,000，**不改 CLAUDE.md 预算上限**。
  总计 59 万 → **68.7 万面**（预算 150 万）、4.11 → **4.73 MB**（预算 40 MB）、
  首屏 1.31 → **1.43 MB**（预算 5 MB）。
- 测试：单测 41 → 44、pipeline pytest 19 → 24、e2e 6 → 7（新增"分享链接选中心脏 +
  背景不被染色"回归用例）。

## 已完成（观感升级 A：数据质量，PR "观感升级 A"）

三步观感升级的第一步，只动数据流水线，不动渲染与界面。

- `groups.yaml` 基准目标面数整体提高：皮肤 60,000、骨骼 5,000（颅骨/髋骨 12,000）、
  肌肉 4,000、器官 10,000（心/肺/肝 15,000）、血管 6,000、脑 15,000
- 新增两条规则（写在 `defaults` 里，人可改）：`max_compression: 2` 源网格远比基准细时
  按最大压缩比放宽目标；`max_target_faces: 30000` 统一封顶，显式基准永远是下限
- `select.py --sync-targets`（默认开）把新目标同步进已定稿的 `content/structures.yaml`，
  "改 groups.yaml → `pnpm pipeline:all`"从此是完整链路
- `process.py`：Taubin 平滑挪到**减面之前**（在原始分辨率抹掉扫描阶梯，减面据此保形），
  伞算子改用 bincount 逐轴累加（几十万面输入下比 np.add.at 快一个量级）
- `export.py`：导出前焊接同位顶点 + 重算面积加权平滑法线，消掉部件接缝的硬棱；
  焊接后的实际面数写回 meta.json，manifest 与 glb 始终一致

**验收数字**：总三角面 **1,090,202**（目标 100–130 万 ✓）、总体积 **7.27 MB**
（≤ 25 MB ✓）、首屏 **2.02 MB**（≤ 4 MB ✓）、`validate.py` 全绿。
对比升级前：68.7 万面 / 4.73 MB / 首屏 1.43 MB。
单结构上限随之改为 30,000、皮肤 60,000（CLAUDE.md 与 DECISIONS.md 同步）。

## 已完成（观感升级 B：渲染，PR "观感升级 B"）

- **舞台**（`src/viewer/stage.ts`）：深色渐变背景球 + 摄影棚三点光（主光定形 /
  补光压暗部 / 冷色轮廓光把身体从背景里切出来）+ 半球光；脚下径向渐变接触阴影，
  高画质档再叠真软阴影（PCFSoftShadowMap + 接影地面，皮肤不投影）
- **后处理**（`src/viewer/postprocess.ts`）：EffectComposer
  `RenderPass →（SSAO）→ OutlinePass → UnrealBloom → OutputPass → SMAA`
- **画质三档**（`src/viewer/quality.ts`）：`low` 不走后处理（软件渲染兜底）、
  `medium` 描边+轻 bloom+SMAA（移动端默认）、`high` 再加 SSAO 与软阴影（桌面默认）。
  界面上的"高画质"开关在视角面板；`?hq=low|medium|high` 可强制
- **材质**：受光材质统一加菲涅尔边缘光并抬一点 alpha——不透明度高时是实体、低时是
  X-ray 壳；器官加 sheen、血管清漆更亮、骨骼提高环境反射；皮肤 X-ray 边缘收窄
- **选中描边**换成 OutlinePass（自带被遮挡部分的描边色），删掉反壳实现
- 曝光 1.12 → 0.98、bloom 阈值 1.05 强度 0.17：实测阈值低了整具骨架会罩白雾

测试：单测 **49**（+5 画质档位）、e2e **8**（+1 画质降级与强制开）。

## 已完成（观感升级 C：界面与内容，PR "观感升级 C"）

- **科普文案换成 `content/definitions/zh.json`**（每条 `blurb` + `fact`，人类成稿）：
  信息卡显示一句话科普 + "你知道吗"小知识；`_meta.reviewed` 为 false 时标注
  "AI 初稿，待人工校对"。旧的 `zh.md` 已删，解析器与双向契约单测同步改写
- **信息卡重做**：系统色点 + 中英名 + FMA、系统/部位/左右标签、blurb、你知道吗、
  来源署名、隔离/隐藏/聚焦
- **3D 标签引线**：选中结构时在旁边挂名字标签，引线连回结构本身，每帧跟相机重投影
- **彩色分层滑块**：轨道按系统色渐变，五个刻度可点击直达该层
- **加载进度条**：首屏加载时占屏进度条（几分之几个系统），首屏就绪后缩成顶部细线
- **过渡缓动**：分层跨度 > 0.08 才缓动（拖滑块跟手、奥秘跳转顺滑），
  相机飞行沿用 easeOutCubic
- **移动端底部抽屉**：信息卡改全宽抽屉（把手、可滚动、安全区内边距），
  顶栏排版修正（品牌不再压住按钮），抽屉弹出时分层滑块与面板标签自动抬到其上
- **本地打包 Noto Sans SC 子集**：`pnpm font:subset` 按 content/ 实际用字
  （1,205 字）子集化，两个字重共 **328 KB**；改中文文案后需重跑

测试：单测 **51**、e2e **9**（+1 信息卡 blurb/fact 与 3D 标签）。

## 已修（2026-08-18，人类反馈"心脏都看不了"）

- **心脏与双肺根本没被画在正确位置**：器官微动画直接覆盖 `mesh.scale/position`，
  把 KHR_mesh_quantization 的反量化变换（肺 116.7 倍）抹掉，三个结构被缩到百分之一
  大小塞回坐标原点。改为 `pulseTransform` 在基准 TRS 之上叠加，+4 个单测
- **隔离没有真的隔离**：其他结构从 0.06 压到 0.015，并在隔离时自动取景到该结构
- **选中的器官是半透明的**：选中/隔离时渲染成不透明（`opacity ≥ 0.999` 关掉 transparent），
  之前 0.85 的心脏后面透出肋骨，形状读不出来
- **高亮把本色洗白**：自发光 0.35 → 0.18，识别度交给 OutlinePass 描边

## 已完成（观感升级 D：器官修形与配色）

针对"心脏近看是个坑洼的蜡块、器官分不出谁是谁"：

- **多件结构改用布尔并集**（manifold3d）而不是拼接三角面。BP3D 一个结构常是几十上百个
  各自封闭的小壳（心脏 83 件、双肺 124 件），拼接会把壳与壳相交处的两层表面和全部内壁
  都留着。并集给出真正的外表面：心脏 10.3 万面 → 8.6 万面且水密，1.3 秒。
  135 个结构 90 个走并集、42 个单件；肝/颅骨/斜方肌有破面，改为"水密的取并集 + 破面的拼回去"
- **逐结构配色表** `content/palette.json`（人可编辑）：心深红、肝红褐、肺粉、胆汁绿、
  软骨蓝灰、脑灰粉……没收录的仍走系统色 + 微抖动
- **分层曲线修正**：器官主场（0.45–0.68）保持不透明，骨骼 0.58 就降到 0.35——
  之前 0.5 不透明度的肋骨压在器官前面，配色全被洗成米白
- 器官材质清漆/光泽/环境反射各收一档，保住颜色饱和度

资产：1,097,031 面 / 7.27 MB / 首屏 2.02 MB，验收数字不变。
测试：单测 **58**（+3 配色契约）、e2e 9、pipeline pytest 29。

## 已完成（观感升级 E：剖切封盖与半剖）

- **剖切模板封盖**（`src/viewer/clipCaps.ts`）：剖开的结构露出实心断面而不是空壳。
  三点成本控制：只给不透明度 > 0.5 且非 X-ray 外壳的结构封盖（器官层约 23 个 →
  多 46 次绘制调用，预算 600）；封盖网格挂成结构网格的子节点，量化缩放与器官微动画
  自动跟上；剖切关掉时整组不渲染
- **`WebGLRenderer({ stencil: true })`**：three r163 起默认 false，不显式打开封盖静默失效；
  EffectComposer 也补了带 stencilBuffer 的渲染目标
- **剖切反向（flip）**：决定切掉哪一半，写进 `?v=` 分享状态。原来固定保留坐标小的一侧，
  从正面剖切时切掉的是背面，看到的还是完整外表面
- **"沿此结构半剖"**：一键把剖切面移到选中结构的中心，并按相机位置自动选方向——
  想看心脏内部最短的一条路
- **桌面端信息卡移到左下**：右侧面板被卡片压住，剖切按钮点不到（实测）

测试：单测 **61**（+3 剖切反解）、e2e **10**（+1 半剖链路）。

## 已完成（观感升级 F：结构层级，打开心脏）

- **一层结构层级**：`groups.yaml` 的组可声明 `parent`，成为父结构的"内部件"。
  心脏先做 8 件，全部来自 BP3D isa 集：心室壁、左/右心房壁、二尖瓣、三尖瓣、
  主动脉瓣、肺动脉瓣、乳头肌
- **交互**：信息卡上"展开内部（8）"→ 父结构隐藏、内部件登场，并自动隔离 + 取景到
  这一家人；点其中一件看它自己的名字与科普；"收起内部"回到整颗心脏
  （右侧面板也有一个，免得没选中结构时收不起来）
- `select.py` 自动把内部件补进 `content/structures.yaml`（顶层结构仍只由人类增删）
- 层级只支持一层、必须同系统：validate.py + manifest 单测双重把关
- **修 `aimAt` 的相机目标 bug**：它拿物体空间的包围盒中心当世界坐标，相机永远对准
  坐标原点。全身视角看不出来（模型正好以原点为中心），一旦缩进去看部件就飞到体外

资产：143 结构 / **1,154,816 面** / **7.67 MB** / 首屏 2.02 MB，仍在预算内。
测试：单测 **64**、pipeline pytest **33**、e2e **11**。

## 已完成（体验收尾 + v0.1 验收清单，M2-6）

- **奥秘升级成分镜**：步骤支持 `expand`（展开内部）与 `clip`（剖切）。
  心跳之旅 7 步 → **10 步**：走到心脏时自动打开内部、点名二尖瓣、再沿正中半剖
  对比左右心室壁厚度。三条契约单测把关（expand 的目标必须真有内部件等）
- **"返回全身"**：一次清掉隔离 + 展开 + 剖切 + 隐藏，并把相机拉回默认取景——
  钻进器官内部之后总得有条出路（原来的"恢复全部"只清显隐，相机还卡在里面）
- **`docs/RELEASE-0.1.md`**：对照 KICKOFF 第 11 节逐条核对，9 条里 6 条已量化达标，
  3 条（10 秒可交互、滑块流畅度、真机观感）标注为只能人类真机确认

**v0.1 规模**：143 结构 / 1,154,816 面 / 资产 7.61 MB / 首屏 2.02 MB；
单测 **67**、pipeline pytest **33**、e2e **12**。

## 已完成（英文科普文案 + 键盘无障碍，v0.1.1）

- **`content/definitions/en.json`**：143 条英文 `blurb` + `fact`，与中文一一对应。
  英文界面下信息卡正文不再是占位句（v0.1 的已知短板第 4 条就此关掉）。
  文案表按语言取（`definitionsFor(lang)`），"AI 初稿"标注也各语言各记各的
- **契约单测扩到双语**：中英覆盖同一批 slug、各自的长度上限（英文一句话字符数
  约为中文两倍，所以上限分开写）、英文里不许残留中文字符
- **键盘可用**（`src/ui/keyboard.ts`，纯函数 `resolveShortcut` + 一个 hook）：
  `/` 搜索、`?` 快捷键说明、`[`/`]` 调透视深度、`1`–`6` 显隐六个系统、
  `F` 聚焦选中、`0` 返回全身、奥秘里 `←`/`→`/空格 走步与播放暂停，
  `Esc` 逐层退出（弹窗 → 奥秘 → 取消选中 → 返回全身）。
  带 Ctrl/Cmd/Alt 的组合一律不接管；焦点在输入框里时除 Esc 外全部放行
- **搜索框键盘化**：↑↓ 走结果、回车选中、Esc 清空（只清搜索框，不冒泡到全局 Esc）；
  结果行按界面语言排主次（英文界面下英文名在前）
- **读屏**：选中结构时用一条 `aria-live` 播报名字（点画布选中原本是"哑"的）；
  搜索框补 combobox/listbox 语义；顶栏加一个 `⌨` 按钮打开快捷键说明（手机端隐藏）
- `StructureInfo.parent` 补进类型定义，`Lang` 类型收敛到 `src/data/types.ts`，
  去掉 InfoCard 里两处 `as` 断言
- 改了中文文案，已重跑 `pnpm font:subset`（子集 1233 字 / 336.8 KB）

测试：单测 **86**（新增 keyboard 10 条、definitions 双语 9 条）、
pipeline pytest **33**、e2e **14**（新增键盘冒烟、英文信息卡）。

## 已完成（接入 HuBMAP HRA 数据源 + 心脏换血，v0.2）

用户点头过许可证后落地的"业余 → 专业"第一步。

- **第二个数据源**：`pipeline/config/sources_hra.yaml`（CC BY 4.0 + 直链 + sha256），
  `download.py` 支持 glb 直下（不解压），产物落在 `pipeline/raw/hra/`（仍不入库）。
  只取 **Male** 版本；**明确不取 `brain-male`**（Allen 脑图谱派生，不在允许清单里），
  `pipeline/tests/test_hra.py` 有一条测试盯着这件事
- **`pipeline/hra.py`**：两件事——坐标系对上（HRA 是米 / Y 向上 / +Z 朝前，
  BP3D 是毫米 / Z 向上 / −Y 朝前；两边 +X 都是被试左侧，用左右心房质心实测确认），
  以及**逐器官等比拟合**（把 HRA 器官的包围盒等比缩放平移到 BP3D 同名结构的包围盒上）。
  为什么不做全局变换：两个数据集是两个不同的人，按身高对齐后心脏中心还差约 18 mm，
  而且 HRA 的皮肤是双臂张开的姿势，X 轴根本不能拿来定标
- **心脏换血**：BP3D 的心脏是扫描分割的 83 片碎壳，"心室壁"只有一层皮、四个瓣挤成一坨；
  HRA 的是医学插画师建的 14 件模型。内部件从 8 个粗件变成 **10 个真解剖结构**：
  左/右心室、左/右心房、**室间隔**、二尖瓣/三尖瓣/主动脉瓣/肺动脉瓣、乳头肌。
  拟合缩放实测 **×0.9771**（两边心脏本来就差不多大），心脏中心与原来完全重合，
  和 BP3D 的肋骨仍然对得上。总面数基本持平（8.78 万 → 8.5 万）
- **本体 id**：结构新增可选 `uberon` 字段。室间隔这类 BP3D 两集都没有的概念只有 UBERON，
  validate 与单测都改成"fma 与 uberon 不能都为空"（许可证铁律的本意是留一个能追溯的 id）
- **署名跟着数据走**：manifest 的 `attribution` 用到哪个源就带哪个源；信息卡的
  "数据来源"按结构的 `source` 显示 BodyParts3D 或 HuBMAP HRA；
  `validate.py` 有一条检查盯着"用了 HRA 就必须署 HRA"，e2e 也断言了一次
- 心跳之旅第 4 步改成点名**左心室**（原来点的是已经不存在的"心室壁"）；
  新增五条中英文案（左右心室、左右心房、室间隔）

资产：**145 结构 / 1,150,600 面 / 7.64 MB / 首屏 2.02 MB**，全部在预算内。
测试：单测 **86**、pipeline pytest **41**（新增 HRA 8 条）、e2e **14**。

## 已完成（HRA 第二批 + 定标方式修正，v0.2.1）

- **又换了五个器官**，各带内部件（新增 11 条，共 156 个结构）：
  - **气管** → 气管软骨环、气管隆嵴
  - **喉软骨** → 甲状软骨（喉结）、环状软骨、会厌软骨
  - **胰腺** → 胰头（含钩突）、胰体、胰尾
  - **脾**（BP3D 原来只有 1330 面，是个粗块）
  - **肾** → 肾皮质（含肾柱）、肾锥体（含肾乳头）、肾门；整肾只取被膜做外表面，
    皮质与锥体放内部件，叠着画会打架
- **肺没换**：HRA 的 `lung-male` 里只有支气管树与肺门，没有肺实质，换过去等于把两片肺删了
- **定标方式改了，推翻上一版的做法**：换第二批时发现 BP3D 的器官网格在前后方向普遍偏薄
  （逐轴尺寸比气管 0.49、胰腺 0.61、肾 0.56，而全身皮肤的前后比是 0.90）。
  按包围盒等比拟合会把 HRA 器官整体缩掉四分之一。改成**全局按身高定标一次**
  （两具身体皮肤网格的身高比 **×0.9425**）+ **逐器官对中心**——
  位置信 BP3D，形状与比例信 HRA。心脏的缩放因此从 ×0.9771 变成 ×0.9425，中心不变
- 两套数据分歧大的器官（脾的长轴方向在两边就不一样）在流水线日志里标出来，只报告不纠正
- 流水线支持一条结构横跨多个 glb（左右肾是两个文件、在本站仍是"肾"一条），
  一条锚点可覆盖一组 glb，缩放对整组算一次

资产：**156 结构 / 1,233,062 面 / 8.36 MB / 首屏 2.03 MB**，全部在预算内。
测试：单测 **86**、pipeline pytest **44**、e2e **15**（新增"打开肾"）。

## 已完成（HRA 第三批，v0.2.2）

- **又换了四个器官**（BP3D 里最粗的那几个），各带内部件：
  - **膀胱**（BP3D 原来 226 面）→ 膀胱三角（含两个输尿管口与膀胱颈）
  - **前列腺**（原来 442 面）→ 外周带 / 移行带 / 中央带。只取分区实质，
    同一个 glb 里的射精管、前列腺小囊、腺导管加起来 19 万面全是内部细管，不收
  - **肝**（整肝取肝被膜做外表面）→ 尾状叶 / 方叶 / 肝门 / 镰状韧带（含肝圆韧带）
  - **脊髓**（原来 728 面）→ 颈髓 / 胸髓 / 腰髓 / 骶髓
- **两条新结构**（BodyParts3D 两集都没有）：**主支气管**、**精囊**
- **锚点算法修正**：只按结构自己声明的部件算包围盒，不再拿整个 glb。
  前列腺暴露的问题——它的 glb 里还装着精囊与输精管，拿整包去比 BP3D 的前列腺
  等于把参照物撑大一圈（逐轴比 0.41/0.75/0.29 → 1.16/1.04/0.57）
- **新增"全身对齐"通道 `fit_from`**，给 BP3D 里没有对应结构的东西用。两种借法：
  ① 借解剖上相连的资产（主支气管借气管的，两者本来首尾相连）；
  ② 借全身皮肤的（缩放照旧按身高，平移按两张皮肤的包围盒中心）。
  **脊髓必须走第 ② 条**：BP3D 的"脊髓"只是脑干下方 36 mm 的一截残桩，
  而 HRA 的是 C1–S5 共 30 节、465 mm 的完整脊髓。用全身对齐之后实测：
  上端 702 mm（在寰椎 695 mm 之上）、下端 264 mm（正好在第一腰椎高度结束），
  与解剖学一致

资产：**170 结构 / 1,295,404 面 / 8.80 MB / 首屏 2.03 MB**。
面数已经贴近 130 万的目标上限，下一批要么提高上限、要么先给现有结构瘦身。
测试：单测 **86**、pipeline pytest **44**、e2e **15**。

## 已完成（HRA 第四批 + 面数预算放宽，v0.2.3）

用户拍板"上限可以更改，最终以项目的完整性和效果优先"，于是先放宽预算再补内容。

- **面数预算**：目标区间 100–130 万 → **100–180 万**，硬上限 150 万 → **200 万**
  （CLAUDE.md 与 validate.py 同步改，理由记在 DECISIONS.md）。
  其余预算不动：资产 ≤ 25 MB、首屏 ≤ 4 MB、draw call ≤ 600
- **消化道补全**：小肠 → 十二指肠 / 空肠 / 回肠；
  大肠 → 盲肠 / 升结肠 / 横结肠 / 降结肠 / 乙状结肠 / 直肠；阑尾也换成 HRA 版
- **泌尿道补全**：输尿管 → 肾盂 / 肾盏（大盏 5 + 小盏 18）。
  现在整条尿路齐了：肾 → 肾盏 → 肾盂 → 输尿管 → 膀胱 → 前列腺
- **呼吸道补全**：新增**支气管树**（叶支气管 + 段支气管 + 肺门，含支气管软骨）。
  气管 → 主支气管 → 支气管树共用一个变换，当一个刚体
- **两条全新结构**：**眼球**（角膜 / 虹膜 / 晶状体 / 视网膜）与**腭扁桃体**。
  眼球有正经锚点——BP3D 的 isa 集里有巩膜、角膜、晶状体三个概念共 6 件网格，
  逐轴比 0.90/1.08/1.05，是所有锚点里最准的之一
- **胸腺**也换了
- **踩到并修掉三处 z-fighting**：取部件时不能让两片面重叠。
  ① 眼球整体原本取"巩膜 + 角膜"，两者在角膜缘重叠 → 只取巩膜；
  ② 视网膜原本含黄斑/中央凹/视盘（贴在视网膜表面的补片）→ 只取本体；
  ③ 虹膜原本含瞳孔（与虹膜共面的薄膜）→ 去掉

资产：**189 结构 / 1,450,023 面 / 10.10 MB / 首屏 2.04 MB**，全部在新预算内。
测试：单测 **86**、pipeline pytest **45**、e2e **17**（新增"气道与眼球"）。

## 已完成（内容质量审核，v0.2.4）

用四个专家 agent 对全部内容做了一轮审核：解剖事实（中文）、英文准确性与中英一致性、
中文文风、脚本结构。107 条发现，**套用 77 条**。

**修掉的事实错误**：
- 肝方叶的两种分叶归属**说反了**（按外形归右叶、按血管归属属左叶）
- "眼球一生尺寸几乎不变"是流传很广的**误说**（出生约 16 mm、成年约 24 mm）
- 英文 `duodenum` 说成 "twelve fingers **wide**"，应为 **long**
- 肺表面积 70 m² 对不上"半个网球场"（网球场约 260 m²），中英都换成了羽毛球场
- `splenius_capitis` 的英文词源说成 splint，实为 splenion（绷带）
- 环状软骨加压当成确定有效的急救做法（指南已不推荐常规使用），改成标明争议
- 乙状结肠"憩室最常发生部位"只适用于欧美人群，已限定
- 脾切除后感染风险被轻描淡写，已改

**修掉的脚本 bug**（这几条观众一定会看到）：
- **心跳之旅第 4 步的剖切参数把整颗心脏切没了**——`clip.pos` 映射的是整具人体的
  包围盒不是结构自己的，写 0.52 让切面落在 y ≈ +76 mm，而心脏在 y ∈ [-72, 27]，
  那 11 秒屏幕是空的
- **神经之旅第 2 步的主角看不见**——垂体属器官层，而这一步 `layer 0.9` 时器官
  已经淡到 0。改成收起神经层、降到 0.7，画面正好是"把大脑收起来露出垂体"
- **神经之旅第 4 步文案与画面打架**——嘴上说"藏在椎骨管道里"，参数却把骨骼关掉了
- **神经之旅第 5 步是第 4 步的同一帧静止 20 秒**，还把"数据将在后续版本补充"这句
  生产备注写进了展词（而且只写在中文里）。整步改成讲脊髓四个节段
- **消化之旅第 5 步与信息卡自相矛盾**（网球场 vs 30 m²，信息卡是对的）；
  第 6 步 `layer 0.85` 让肝脏完全不可见，可整句话讲的就是肝脏

**固化成自动化测试**（`tests/unit/wonders.test.ts`，新增 5 条）：剖切面必须与主角 bbox
相交、主角在该分层下不透明度 > 0.15、相邻两步画面必须有变化、展词里不许有生产备注、
中英文案长度比不得低于 1.2。**这五条测试直接抓出了上面三个 bug**，其中垂体那条是
写完测试后新发现的。

**`docs/CONTENT-GUIDE.md`**：把审核结论固化成写作规范——信息卡的字段与硬规则、
脚本的"五拍骨架"、`durationMs` 公式、中英并行要求，以及规模化之前必须先做的
七件事（文件名约定、元数据、结构反向索引、`systems` 数值化、机位解耦、相机 flyTo、
UGC 前置）。

测试：单测 **91**（+5）、pipeline pytest 45、e2e 17。

## 已完成（更名「奥秘」+ 按结构关联，v0.3）

用户定的内容方向落地第一步：内容单元将成为项目的主要资产，丰富到每个器官和局部，
点开结构时关联展示，后续支持 UGC 与动态分享。

- **更名**：「故事线 / Tour」→「**奥秘 / Wonder**」。用户否掉了旅程/导览/探秘/脉络
  四个候选（"科普的意境不强"），最终选定「奥秘」——中文科普的黄金词组，
  英文 Wonder 同样是科普黄金词，全球读者都懂。
  改动面：`content/tours/` → `content/wonders/`、`src/tours/` → `src/wonders/`、
  `Tour*` → `Wonder*`、i18n 键与文案、CSS 类名、测试 id、全部文档
- **自动收录**：`src/wonders/index.ts` 改用 `import.meta.glob`，加一个 JSON 文件即生效，
  以后上百条不必逐条登记。顺序按文件名排，不受打包器遍历顺序影响
- **元数据**：每条奥秘声明 `system`（菜单分组用）与 `structures`（涉及的结构）；
  时长由 `estimatedSeconds()` 从 `durationMs` 求和算出，不再手填
- **按结构关联**：`wondersForStructure(slug, parent)` 建反向索引。
  **信息卡上现在显示「关于它的 N 则奥秘」，点一下直接开播**；
  内部件（瓣膜、心室）没有自己的内容时回退到父结构，不会推荐不出东西
- 契约测试跟上：每条奥秘的 `system`/`structures` 必须齐全且结构真实存在、
  每一步点名的主角都要在声明里（否则反向索引会漏）、反向索引的回退行为、
  没人讲到的结构不许瞎推荐

测试：单测 **93**（+2）、pipeline pytest 45、e2e **18**（+1：信息卡列出相关奥秘并能开播）。

## 已完成（奥秘的规模化地基，v0.3.1）

按"先把地基做完再批量写内容"的顺序，`docs/CONTENT-GUIDE.md` 第三节的六件事全部落地：

- **`?wonder=<id>` 深链**：直接开播某一则奥秘，分享链接与展厅排期都靠它。
  id 不存在时安静回到普通视图，不炸
- **菜单按系统分组**：按分层顺序（由外到内）分组，每条显示时长（由 `durationMs` 求和算出）
- **`systems` 支持数值**：`false` 关掉、数字 0–1 压暗、缺省完全可见。
  "在肋骨里面看心脏"写成 `systems: { skeleton: 0.2 }`——原来只能二选一，
  关掉骨骼就没有"在里面"了。退出奥秘时会还原，不会留下一屏灰
- **机位方向与距离解耦**：新增 `from` 只定方向，距离仍由 `focus` 按结构包围盒算。
  "从左侧凑近看二尖瓣" = `from: 'left'` + `focus: true`；`preset` 保留原义
  （整具人体宽景）。原来两者互斥，小结构永远只能从上一步的角度看
- **收起时相机改成飞回去**：`frameContent` 只在首次加载时硬切，收起隔离/展开时
  改为 `flyTo`，不再弹一下
- **UGC 前置**：`content/schema/wonder.schema.json`（JSON Schema draft 2020-12）
  是将来投稿的校验依据，**内置内容自己先跑这条校验**（用 ajv）——否则外部作者
  按 schema 写出来的东西会和内置的不是一回事。每条奥秘带 `schema: 1` 版本号

测试：单测 **94**（+1：schema 校验）、pipeline pytest 45、e2e **19**（+1：`?wonder=` 深链）。

## 已完成（观感与交互修复，v0.3.2）

用户反馈原话："两边眼球不一样，标注位置也不对，人体不是以鼠标为中心点，
放大缩小和移动都很不方便，移动端更是如此，页面设计杂乱。" 五条逐条处理：

- **眼球碎裂（数据层）**：HRA 的 glb 有一部分是**未焊接**的（眼球巩膜 12,192 面
  却有 24,383 个顶点），`fast_simplification` 把它当成一堆互不相连的三角形，
  减完碎成 **469 块**——就是画面上的麻点。改成"逐部件焊接 → 按面数比例分配预算
  逐部件减面 → 清碎屑"（`hra.simplify_parts`）。顺带修好大肠 6,229 → 17 块、
  支气管树 2,514 → 40 块。
- **碎屑判据推倒重来**：第一版按"占整个结构总面数 0.5% 以下就是碎屑"一刀切，
  把晶状体悬韧带（322 根各 21–42 面的睫状小带，是**真解剖**）删掉 95.2%。
  现在判据按**部件自身**的尺度定，并且先用 `clean_source` 扫掉源网格里的建模垃圾
  （右心房焊完有 1,060 块，其中 1,058 块是 1–2 面、对角线为 0 的零面积三角形，
  它们会把"最小连通块"拉到 1 面，让判据整个失效）。右心房 1,070 → 6 块，
  晶状体 306 块全留。悬韧带逐根减不下去，`target_faces` 改成实测下限 12,000。
- **标注落点**：原来引线指向包围盒中心，成对结构（两只眼球、两颗肾）的包围盒中心
  落在两者**之间的空气里**，引线直指颅骨正中。新增 `src/viewer/anchor.ts`：
  先从相机往包围盒中心打射线，打中自己就用命中点；打空了退回顶点抽样，
  在离相机最近的那批点里挑屏幕上离中心最近的一个。
- **相机交互**：`zoomToCursor = true`（滚轮朝哪儿滚就往哪儿去）、
  最近距离 200 mm → **25 mm**（原来聚焦晶状体算出 55 mm 会被夹回 200 mm，
  小结构永远只有指甲盖大）、双击/双指双击把轨道中心挪到点中的那一点并凑近一半、
  显式声明触屏手势（单指转、双指捏合缩放+平移）。
- **界面减负**：右侧两块常驻大面板（六行滑块 + 十个按钮 + 一段画质说明）收成
  **三条标题栏**（系统 / 视角 / 剖切），一次只展开一格，默认全收起；
  搜索框收成顶栏上的一个放大镜（`/` 仍可唤出）；分享/署名/帮助改成内联 SVG 图标
  （中文字体是按用字裁的子集，⌕ ↗ ⓘ 不在里面）；画质开关挪进帮助面板。
  小屏上三格横过来贴在分层滑块上方，展开的那格从上方弹出。

- **顺带修的几条**：近远裁剪面改成每帧跟着观察距离走（原来只在取景时算一次，
  凑到 25 mm 就整个被裁掉）；聚焦的尺寸下限 60 mm → 15 mm（晶状体原来算出 139 mm，
  屏幕上只占一成高）；预设视角在隔离/展开时框住那一块而不是整具人体；
  抽屉不再因为选中结构自动收起（桌面端刚调完不透明度就得重开，很别扭）。

资产：三角面 **1,441,469**（+8,236，眼球与心脏的细节找回来了），9.63 MB，
首屏 2.04 MB，均在预算内。测试：单测 94、pipeline pytest 45、e2e 19。

## 已完成（取景与苹果设备适配，v0.3.3）

用户在苹果笔电和手机上问"人体不在居中位置"。**实测下来人体一直是居中的**
（六个苹果分辨率下水平偏移只有 5~11 px），真问题是它占满了视口高度的 **90%**，
上下各只剩三四十像素——脚陷进底部分层滑块（MacBook Air 13 上压进去 65 px），
手机上头顶还压着通栏的品牌栏。看得见的空白不对称，视觉重心自然偏下。

苹果笔电是 16:10 屏，扣掉菜单栏和 Safari 标签栏后可视高度特别矮
（Air 13 只有 789 CSS 像素），把这个毛病放大了；16:9 的机器同样有，只是轻些。

- **按安全区取景**：`poseForBox` 接受安全区参数，按界面之外剩下的空当取景，
  并把人体中心对齐到**安全区中心**。方向反直觉——想让画面内容往上移，
  相机得往下挪，注释里写清楚免得被"修反"
- **安全区量真实 DOM**：`src/ui/useSafeInsets.ts` 量品牌栏/顶栏/分层滑块/
  工具抽屉/奥秘播放器再推给渲染核心。面板高度随语言、字号、播放状态变，
  写死必然对不上；转屏、拉窗口、开播奥秘都会重量。渲染核心不认识 React，
  也就不该猜面板有多高
- **余量 1.15 → 1.05**（先试过 1.28，人体只剩五到六成高，太小）。实测占比：
  MacBook Air 75% / Pro 14 77% / Pro 16 80% / iPhone 14 70% / SE 65% / iPad 81%，
  脚离滑块 17 px、手机上离抽屉 14 px
- **别顶掉已聚焦的相机**：`setSafeInsets` 重取景的守卫原先只挡隔离/展开，
  而分享链接 `?v=selected=heart` 是"选中并聚焦"，相机刚飞到心脏就被顶回全身。
  改成跟踪 `autoFramed`，`flyTo` 一调用就置 false

苹果特有的四条，补了三条：

- **WebGL 上下文丢失完全没接**（最要紧的一条）。iOS Safari 切后台、来电话、
  内存紧张时会回收 GPU 上下文，画布永久空白只能刷新。关键是丢失时必须
  `preventDefault()`，**不调用浏览器根本不派发 restored 事件**；恢复时要重建
  后处理链（EffectComposer 的渲染目标随旧上下文一起没了）
- **iPhone 底部安全区**：`index.html` 声明了 `viewport-fit=cover`，贴底的分层滑块
  与工具抽屉不加 `env(safe-area-inset-bottom)` 会被 home 指示条压住
- **iOS Safari 的 `vh` 指的是工具栏收起后的高度**，比可见区域大一截，抽屉与
  信息卡改用 `dvh`（`@supports` 兜底）
- 未改：Retina 像素比上限 2，M 系列扛得住，Intel 老 MacBook 需手动关高画质

另补 **WebGL2 能力检测**：three 0.185 没有 WebGL1 兜底，原来落到"模型加载失败，
请刷新重试"——刷一万次也没用。现在明确说不支持 WebGL2 并给出浏览器版本要求。

## 已完成（Retina 画布事故，v0.3.4）

用户在 iPhone 上打开只看到一点点人体（大片空背景 + 右下角露出一点头部）。

根因：`renderer.setSize(w, h, false)` 的第三个参数是 `updateStyle`，传 `false` 时
three 只设 `canvas.width/height`（绘制缓冲 = CSS 尺寸 × 像素比），**不设
`canvas.style.width/height`**；而 `ui.css` 里主画布没有任何尺寸规则。于是 canvas
按固有尺寸显示——DPR 1 恰好等于容器尺寸，DPR 2 的设备（**所有 Retina 苹果设备、
多数手机**）画布被撑成容器的两倍，左上角对齐，只看得见整幅画面的左上四分之一。

- 改回默认的 `updateStyle`，并在 `ui.css` 加兜底规则
  `[data-testid='viewer'] > canvas { width: 100%; height: 100% }`
- 顺带修工具抽屉压着分层滑块：滑块占底部 8~98 px，抽屉 `bottom` 是 82 px，
  重叠 16 px，改成 106 px
- **补 retina e2e 用例**：Playwright 默认 `deviceScaleFactor: 1`，19 条冒烟全瞎，
  这个 bug 一路绿灯上线。新用例在 DPR 2 下断言画布尺寸与容器一致（误差 ≤1 px）

DPR 2 复验（iPhone / 微信内置浏览器四档高度）：人体竖向占比 54~71%，
与顶栏、工具抽屉均不重叠（脚离抽屉 12 px）。e2e 20/20。

## 已完成（内容规模化与移动端界面，v0.4）

### 十则新奥秘：覆盖率 30% → 59%

肌肉（42 个结构）与骨骼（35 个）此前一则奥秘都没有，点开股四头肌或股骨，
信息卡上「关于它的 N 则奥秘」是空的。新写 `breathing` / `spine` / `standing` /
`reach` / `urine` / `vision` / `voice` / `hand` / `aorta` / `ribcage` 十则，
189 个结构里 112 个（59%）点开有内容可推。

### 四个专家 agent 审核，168 条发现全部处理

- **解剖事实 44 条**，其中 12 处是真错误：晶状体调焦机制写反了（睫状肌收缩时
  悬韧带松弛、晶状体靠自身弹性变鼓，不是「肌肉把它拉扁」，这是流传最广的
  反向误说）；手骨占比算错一倍；胸椎「最不能扭」实为腰椎；主动脉分叉在 L4 而非
  骨盆上缘；颈动脉搏动写成下颌角下方（那里接近颈动脉窦，按压可能诱发心率骤降）；
  气管「被人掐住不会塌」不成立
- **英文 24 条**：具名肌肉被泛化、垂悬分词、`-ward` 未统一成英式 `-wards`
- **中文文风 52 条**：21 处复述信息卡（最长逐字重合 14 字）、10 条收尾里 8 条
  落在两种句型上、断句陷阱（「一分为二成髂动脉」会念成「二成」）
- **脚本结构 48 条**：用真网格做屏幕空间遮挡光栅化，量出「讲的东西观众看不见」
  —— 肾锥体 0.0%（整个躲在肾皮质里）、肱二头肌 4.1%、胫骨前肌 10%。
  根因是 `from` 会被后续步骤继承，某一步写了 `from: 'back'`，后面几步的正面
  肌肉就跟着在背面拍了

### 三条新测试：把靠自觉的规则变成机器把关

| 测试 | 抓什么 |
|---|---|
| 选中内部件必须展开父结构 | 不透明度直接为 0 那一类 |
| 展词不许复述信息卡（最长公共子串 ≤ 7 汉字） | 21 处复述 |
| 主角被兄弟件套住必须剖开 | 0% 可见那一类 |

第三条特意验证过会红：临时移除 `urine[4]` 的剖切，测试立刻指名
「kidney_pyramids 被 kidney_cortex 套住」。第二条当场抓到我自己的改动。

### 移动端：七处修复

用户在微信里点开「左髋骨」，引线朝右下指进界面底下。实测 402×640 视口：
三维可见高度只剩 156 px（24%），信息卡独占 326 px（51%），
滑块下沿还压在卡片底下 22 px。

- `poseForFocus` 完全不接安全区——开屏取景上一轮已经改了，但**点选结构走的是
  另一条路**，它把结构居中到整块画布，而画布下半部被卡片盖着
- `.hyi-info` 不在安全区清单里。判据没用媒体查询猜断点，而是**只算横向盖住
  画布中线的界面**：桌面端左下角的卡片、右上角的抽屉都在人体两侧，本不该挤窄
  取景；同样两个元素在手机上是通栏的，就必须算进来
- 手机上卡片顶部已经大字写着结构名，三维里的浮动标签是重复，不画
- 卡片盖住半屏时收起工具抽屉（省 90 px）
- CSS 写死 `46vh` 让位，而卡片实际 326 px，改成量真实高度写进 `--hyi-info-h`
- `safeFraction` 原来「安全区不足 30% 就放弃调整」，而卡片打开时正好 27.8%
  ——最需要它的那一刻撂挑子。改成再窄也照样平移，只是距离不再按比例拉远
- **聚焦前先把分层挪到该结构看得清的位置**：原来搜索选中「左髋骨」，相机飞进
  身体里而分层还停在「皮肤」，骨骼只有 0.22 不透明度，满屏半透明组织糊成一团

三维可见高度 156 → 178 px。

**已知局限**：骨骼的主场分层（0.45）上器官也是满不透明的，选中盆骨这类被肠管
挡住的骨头时靠选中描边勾出来。分层曲线里没有哪一档能让骨骼亮而器官暗，要改得动
`layers.ts` 的整体设计。另注意云端软件渲染是 low 档、**描边是关的**，云端截图
看不出高亮效果，这一条只能真机判断。

## 未完成 / 下一步

- **继续写奥秘**：13 则、覆盖 59%。仍完全没有内容的：肌肉 26 个、骨骼 21 个、
  血管 17 个、器官 12 个。再写 8~10 则可到 80%
- **信息卡两段式抽屉**（未做，等人类拍板）：手机上卡片固定 326 px 占屏 51%，
  是三维可见高度从 28% 提到 50% 的唯一大杠杆。默认只露标题 + 一行科普 + 操作按钮
  （约 160 px），上滑展开全文。会改变一个已经用熟的交互，所以先不动
- **分层滑块瘦身**（未做）：五个系统标签独占一行约 40 px，可改成只在拖动时浮现
- **UGC 还差**：`level`（少儿 / 成人）、投稿与审核流程、作者署名与许可证声明
- 肺仍然换不了；周围神经仍然缺（需要新数据源，要先过许可证）
- 键盘还不能用 Tab 在三维结构之间走

## 给人类的待办

1. ~~**`v0.1` 标签**~~ **2026-08-19 已完成并收尾**：`refs/tags/v0.1` → `80e5cd7`，
   Release 已发布并标记 Latest，临时分支 `release/v0.1` 已由人类删除。
   过程与云端代理的 ref 权限记在 `docs/RELEASE-0.1.md`。
2. **真机确认帧率**：面数已到 145 万，KICKOFF 要求中端安卓 30 fps。
3. **校对文案**：`content/definitions/{zh,en}.json` 各 189 条已过一轮 AI 审核，
   `_meta.reviewed` 仍是 `false`。**改动中文后记得跑一次 `pnpm font:subset`**。
4. **CI 未跑 Python 测试**：`.github/workflows/ci.yml` 属预置文件本会话未改。
5. PWA 图标仍是占位色块。
6. **可选**：远端还留着六个已合并的旧特性分支（`…-a-data` / `-b-render` / `-c-ui` /
   `-d-organs` / `-e-clipcaps` / `claude/hyibody-setup-7eumlp`），想清就在
   <https://github.com/duyuanping9798/hyibody/branches> 删掉；云端代理拒绝删分支操作。
