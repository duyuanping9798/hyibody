# dependencies / build
node_modules/
dist/

# tests
test-results/
playwright-report/
coverage/

# pipeline data (never commit raw data, see CLAUDE.md)
pipeline/raw/
pipeline/work/
__pycache__/
.pytest_cache/

# placeholder glb is generated at build time only when no pipeline manifest exists
# (scripts/generate-placeholder.mjs); pipeline outputs in public/assets/ are committed.
public/assets/placeholder.glb

# misc
.DS_Store
*.log
.env
.env.*
.font-cache/
